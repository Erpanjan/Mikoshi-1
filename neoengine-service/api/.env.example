# API Secret for generating and verifying API keys
# This should be a strong, random secret key
API_SECRET=your-secret-key-here-change-this-in-production

# Google Cloud Storage Configuration
GCS_BUCKET_NAME=neo-engine-asia-east2
GCS_PROJECT_ID=voluntas-dev

# Optional: Path to GCS service account credentials JSON file
# If not provided, will use default credentials (e.g., from Cloud Run)
GCS_CREDENTIALS_PATH=gac.json

# Server Configuration
HOST=0.0.0.0
PORT=8080

